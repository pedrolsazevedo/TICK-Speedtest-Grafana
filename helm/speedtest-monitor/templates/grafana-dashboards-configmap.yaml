apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ .Release.Name }}-grafana-dashboards
  labels:
    {{- include "speedtest-monitor.labels" . | nindent 4 }}
data:
  speedtest.json: |-
{{ .Files.Get "dashboards/speedtest.json" | indent 4 }}
